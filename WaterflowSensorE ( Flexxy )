#define BLYNK_TEMPLATE_ID "TMPL67L5v5I01"
#define BLYNK_TEMPLATE_NAME "Water Flow Monitor"
#define BLYNK_AUTH_TOKEN "zh3jWhm0FaK45niHGNj-35-pLyfbuaWs"

#include <WiFi.h>
#include <BlynkSimpleEsp32.h>

char ssid[] = "Fmz";
char pass[] = "iiiiiiii";

#define FLOW_SENSOR_PIN 27
#define LED_PIN 26

volatile int pulseCount = 0;
float flowRate = 0;

BlynkTimer timer;

void IRAM_ATTR pulseCounter() {
  pulseCount++;
}

void sendFlowRate() {
  noInterrupts();
  int count = pulseCount;
  pulseCount = 0;
  interrupts();

  float flow_L_per_min = count / 7.5;
  flowRate = flow_L_per_min / 60.0;  // L/sec

  Serial.print("Flow rate: ");
  Serial.print(flowRate, 3);
  Serial.println(" L/sec");

  Blynk.virtualWrite(V0, flowRate);
}

void setup() {
  Serial.begin(115200);

  pinMode(FLOW_SENSOR_PIN, INPUT_PULLUP);
  attachInterrupt(digitalPinToInterrupt(FLOW_SENSOR_PIN), pulseCounter, FALLING);

  pinMode(LED_PIN, OUTPUT);

  digitalWrite(LED_PIN, LOW);   // 🔥 LED ALWAYS ON (active LOW wiring)

  Blynk.begin(BLYNK_AUTH_TOKEN, ssid, pass);

  timer.setInterval(1000L, sendFlowRate);
}

void loop() {
  Blynk.run();
  timer.run();
}